🚀 YS-GRE PRO (Advanced Tunnel & Forwarder)
ترکیبی از هسته قدرتمند تانل GRE سپهر و سیستم مانیتورینگ هوشمند YSGUARD.

🌟 قابلیت‌های کلیدی:
GRE Tunneling: برقراری تانل لایه ۳ با منطق دستی (مناسب سرورهای پشت NAT).

HAProxy Integration: فوروارد کردن پورت‌ها با پایداری لایه ۴ و ۷.

Smart Healer: چک کردن لحظه‌ای پینگِ داخل تانل و ریست خودکار اینترفیس در صورت قطعی.

Telegram Notify: ارسال گزارش‌های قطعی و بازیابی تانل به بات تلگرام شما.

Kernel Optimizer: اعمال خودکار پروتکل BBR و بهینه‌سازی‌های شبکه.

🛠 دستور نصب تک‌خطی (One-Liner):
ابتدا فایل را در ریپازیتوری خود آپلود کنید، سپس با دستور زیر اجرا کنید (جایگزین کردن USER و REPO با اطلاعات خودتان):

Bash
bash <(curl -Ls https://raw.githubusercontent.com/Rmin-ys/YSGRE/main/install.sh)
📖 راهنمای گام‌به‌گام:
مرحله ۱ (Setup GRE): در هر دو سرور اجرا شود. آی‌پی داخلی (Local) و آی‌پی پابلیک سرور مقابل (Remote) را وارد کنید.

مرحله ۲ (Add Port): فقط در سرور ایران پورت‌های مورد نظر را وارد کنید تا به خارج فوروارد شوند.

مرحله ۳ (Monitor): توکن بات و چت‌آیدی را بدهید تا هیلر و گزارشگر تلگرام فعال شوند.

مرحله ۴ (Status): همیشه برای اطمینان از برقراری پینگ بین دو سرور، این گزینه را چک کنید.

⚠️ نکات امنیتی و فنی:
آی‌پی داخلی (Local IP): اگر سرور شما در دیتاسنترهایی مثل ابرآروان یا شاتل است، حتماً آی‌پی اینترنال (رنج ۱۰ یا ۱۷۲) را وارد کنید.

رنج تانل: این اسکریپت از رنج 192.168.168.0/30 برای شبکه داخلی تانل استفاده می‌کند تا با وایرگارد (10.0.0.x) تداخل نداشته باشد.

بدون نیاز به ریبیلد: تمام تنظیمات قابل حذف (Uninstall) هستند و آسیبی به ساختار اصلی سرور نمی‌زنند.
